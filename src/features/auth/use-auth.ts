export const useAuth = () => {
  const register = () => {};

  const login = () => {};

  const logout = () => {};

  const unregister = () => {};

  return {
    register,
    login,
    logout,
    unregister,
  };
};
